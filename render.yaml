services:
  - type: web
    name: crimzn-consult
    env: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 16.20.2
